<%
headers = [
    "User",
    "Data",
    "Destino",
    "Total",
  ]

  headers1 = [
    "Tipo",
    "Valor do custo"
  ]

  csv_output = CSV.generate do |csv|
    csv << headers

      csv << [
        current_user.name,
        @viagem.date.strftime("%d-%m-%Y"),
        @viagem.name,
        @total       
      ]
  end

  csv_output2 = CSV.generate do |csv|
    csv << headers1

      @viagem.custos.each do |custo|
      csv << [
        
          custo.tipo.description,
          custo.valor
        ]
      
    end
  end


%>

<%= csv_output.html_safe %>
<%= csv_output2.html_safe %>